# BOARDROOM 2026 — v0.9 UX 수정안 (검토용): 이해도·반복 제거·회의록 타임라인

작성일 2026-09-13 · 대상 기준: main 커밋 138e275 (P0 + 디자인 마감 머지 상태) · 형식은 v0.6/v0.7 수정안과 같다: `대상 → 현재 → 문제 → 수정안 → 적용 후 확인`

## 0. 계기와 검토 방법

Render 배포본을 실제로 체험한 뒤 나온 피드백 두 가지다.

1. 안건을 고르고 자료 E1~E4를 읽고 의견을 내는 흐름이 **잘 이해되지 않는다.**
2. 의견(DISCUSS)과 추가 의견(REACTIONS 후속)을 두 번 내는 것이 **반복처럼 느껴진다.** 아예 실제 회의처럼 흘러가는 **채팅 형식**은 어떤가.

이 문서는 원인을 진단하고 세 가지 안(A 최소 수정 / B 회의록 타임라인 / C 완전 채팅형)을 비교한 뒤 권장안을 낸다. **표결 규칙·조건 ID·세션 reducer는 바꾸지 않는다.** 바뀌는 것은 화면의 프레이밍·순서·입력 방식이다. 검토는 두 갈래로 한다: (a) Codex가 PR #3에서 구현 관점(240초 예산·접근성·scripted/live 동등성·규칙 엔진 영향)을 검토하고, (b) GPT가 기획 관점에서 각 항목을 채택/수정 채택/기각으로 판정해 `docs/REVISION_DECISIONS_v0.9.md`에 남긴다.

---

## 1. 진단 — 왜 브리핑에서 의견까지가 어려운가

- **대상:** BRIEFING·OPINIONS·DISCUSS 화면, `docs/SCENARIO_AI_ASSISTANT.md` 브리핑 절.
- **현재:** 원안 한 문장 → 자료 카드 4장(각 한 줄) → AI 정리 카드 → [의견 듣기] → 임원 4명의 한 줄 의견 → [내 의견 말하기] → 추천 문구 6개 + 300자 입력.
- **문제:**
  1. **결정 질문이 없다.** "무엇을 정하는 회의인가"가 화면에 없다. 원안은 "AI 업무 비서를 도입한다…미정이다"로 끝나 참가자는 찬반 대상을 스스로 추론해야 한다. 의장 대사(`chairLine`)는 시나리오에만 있고 화면에는 없다.
  2. **자료가 결론 없이 나열된다.** E1 "120건, 검토 완료", E2 "126건, 검토 전"은 숫자 불일치를 스스로 발견해야 의미가 생긴다. 각 카드에 "이 자료가 말하는 것"(찬성 근거인지 우려 근거인지, 어느 임원이 이걸 근거로 삼는지)이 없다.
  3. **조건(PILOT·REVIEW·ACCESS·MEASURE)이 너무 늦게 나온다.** 이 체험의 실제 레버는 "어떤 조건을 붙여 안건을 고칠 것인가"인데, 조건은 DISCUSS의 추천 문구를 고른 뒤에야 칩으로 드러난다. 참가자는 브리핑 시점에 "내가 할 수 있는 일"을 모른다.
  4. **역할 안내가 없다.** 명패 "나 · 특별 이사"는 있지만 "특별 이사가 이 4분 동안 하는 일 = 조건을 붙여 의견을 내고 한 표를 던진다"가 어디에도 없다. 진행 단계도 헤더 pill 하나뿐이라 전체 여정이 보이지 않는다.
  5. **4분 타이머가 브리핑부터 돈다.** 읽는 동안 시간이 줄어 서둘러 넘긴다.
- **수정안(A-1, 모든 안에 공통):**
  1. BRIEFING 상단에 **의장 브리핑 블록**을 둔다: 상황 한 문장("주간 보고마다 부서 자료를 손으로 모읍니다. 이걸 AI 비서에 맡길지 정합니다"), 결정 질문("도입할까요? 한다면 어떤 조건을 붙일까요?"), 특별 이사의 역할 한 줄("이사님은 조건을 붙여 의견을 내고, 마지막에 한 표를 던집니다"). 시나리오의 `chairLine`을 화면에 올리고 두 문장을 더한다.
  2. 자료 카드 4장에 **"이 자료가 말하는 것"** 한 줄과 **관련 임원 아바타**를 붙인다. 예: E1·E2 "같은 일의 건수가 자료마다 다르다 → CIO: 출처·기준일 확인 필요", E3 "취합이 반복된다 → CEO·CFO: 자동화 효과 기대", E4 "권한 설계가 안 끝났다 → CISO: 권한 확인 먼저". 시나리오 문서의 기존 표를 데이터로 옮기므로 새 사실을 만들지 않는다.
  3. AI 정리 카드를 **"핵심 쟁점 3개"** 형식으로 바꾼다: ① 숫자가 자료마다 다르다(E1·E2) ② 검토 담당자가 없다(E3) ③ 권한·공유 범위가 미정이다(E4). 문장은 현재 `briefingSummary`를 그대로 쪼갠 것이다.
  4. BRIEFING 하단에 **조건 미리보기** "이사님이 붙일 수 있는 조건"으로 PILOT·REVIEW·ACCESS·MEASURE 라벨 4개를 읽기 전용 칩으로 보여 준다. 클릭은 DISCUSS에서 한다.
  5. 헤더 아래에 **진행 스트립** "① 상황 파악 → ② 임원 의견 → ③ 내 의견 → ④ 반응에 답하기 → ⑤ 표결"을 상시 표시하고 현재 단계를 강조한다.
  6. **타이머는 BRIEFING을 읽는 동안 멈추지 않는다**(240초 규칙 유지)가, BRIEFING의 [의견 듣기] 옆에 "남은 시간은 충분합니다(권장 40초)"를 표시해 재촉감을 줄인다. 시간 규칙은 바꾸지 않는다.
- **적용 후 확인:** 처음 보는 사람 3명이 설명 없이 BRIEFING을 읽고 "무엇을 정하는지"와 "내가 뭘 할 수 있는지"를 한 문장으로 말할 수 있어야 한다(현장 검수 항목으로 FACILITATOR_GUIDE에 추가).

## 2. 진단 — 왜 의견 두 번이 반복처럼 느껴지는가

- **대상:** DISCUSS·REACTIONS 화면, 시나리오 "첫 반응 및 후속 질문" 절.
- **현재:** DISCUSS에서 문구 선택 + 300자 입력 + 조건 칩 확인 → [의견 전달]. REACTIONS에서 내 발언 인용 + 임원 반응 4장 + 후속 질문("메일과 실적표의 숫자가 다르면…") + 선택지 3개 + 300자 입력 + 조건 칩 → [보완 의견 전달] 또는 [앞선 의견 유지].
- **문제:**
  1. 두 화면의 **입력 폼이 같은 모양**(선택지 + 큰 textarea + 칩)이라 "같은 걸 또 시킨다"로 읽힌다.
  2. 후속 질문의 **목적이 화면에 없다.** 이 질문은 "숫자 불일치를 어떻게 다룰지"로 REVIEW 조건을 유도하는 장치인데, 누가 왜 묻는지 표시가 없다.
  3. 임원 반응이 한 줄씩 카드로 나열되어 **대화가 아니라 게시판**처럼 보인다. 내 발언에 대한 응답이라는 인과가 약하다.
- **수정안(A-2):**
  1. REACTIONS의 후속 입력을 **의장의 질문 + 빠른 답 3개**로 줄인다. textarea는 기본 접힘("직접 답하기"를 눌러야 열림). 조건 칩은 답을 고른 뒤에만 나타난다. 기본 강조 버튼은 "앞선 의견 유지"가 아니라 첫 번째 선택지로 두되, 어느 것도 미리 선택하지 않는다.
  2. 후속 질문에 **발화자와 이유**를 붙인다: "CIO가 묻습니다 — 자료마다 숫자가 다른데, 그대로 공유해도 될까요?" 시나리오의 공통 질문을 CIO 발화로 귀속(근거 E1·E2와 일치).
  3. 임원 반응 카드를 **내 발언 아래에 답글 형태(들여쓰기·연결선)**로 배치하고, 변한 임원만 강조하고 나머지는 "기존 의견 유지"로 흐리게 한다(현재 라벨 유지).
  4. 화면 제목을 "임원들의 반응"에서 **"이사님 의견에 대한 반응 — 한 가지만 더 여쭙겠습니다"**로 바꿔 두 번째 입력이 '보완'이 아니라 '질문에 답하기'임을 드러낸다.
- **적용 후 확인:** 후속 단계에서 텍스트 입력 없이 버튼만으로 완주 가능(E2E "직접 입력 없이 완주" 유지). 후속 단계 평균 체류가 40초 이하.

## 3. 세 가지 안 비교

| | A. 최소 수정(위 1·2절) | B. 회의록 타임라인(하이브리드) | C. 완전 채팅형(자유 대화) |
| --- | --- | --- | --- |
| 화면 | 현재 9화면 유지, 프레이밍·카피·후속 입력만 변경 | BRIEFING~REACTIONS를 **한 개의 스크롤 타임라인**으로 통합. 의장·임원·나의 발언이 시간순으로 쌓이고, 내 차례에 추천 문구(quick reply)와 작성기가 아래에 뜬다. MOTION·VOTE·RESULT는 유지 | 자유 텍스트 N턴 왕복, 임원이 매 턴 응답 |
| 도메인·규칙 | 변경 없음 | 변경 없음(단계 전이는 그대로, 렌더만 통합) | 조건 추출을 자유 대화에서 해야 함 → 규칙 엔진 신뢰성 저하 |
| 240초 | 그대로 | 그대로. 스크롤이 길어지지 않게 단계별 접기 필요 | 타이핑 속도(서서, C레벨)로 2~3턴이 한계. 실질적으로 지금보다 짧은 대화 |
| live 모드 | 그대로 | "판단 중" 타이핑 인디케이터가 자연스러움. 라운드당 8초 대기가 흐름 안에 흡수됨 | 턴마다 8초 대기 → 체감 지연 큼. 32초 이상 대기 가능 |
| scripted 모드 | 그대로 | 그대로(대본이 대화처럼 보여 오히려 유리) | 대본으로는 자유 대화를 못 만듦. scripted에서는 빈약 |
| 검수·안전 | 그대로 | 그대로 | 검수 불가 발언 비율 증가, 주입·탈선 위험 증가 |
| 접근성·키보드 | 그대로 | 타임라인 포커스 관리 필요(새 발언에 live region) | 채팅 입력 중심이라 키보드 완주는 가능, 문구 선택 접근성은 약화 |
| 공수 | 1~2일(카피·CSS·소규모 마크업) | 3~4일(화면 통합·스크롤·live region·E2E 수정) | 1주 이상 + 스펙 재작성 |
| 재미·몰입 | 이해도 개선 | **회의에 앉아 있는 느낌**이 가장 강함 | 자유도는 높지만 4분 안에서는 산만 |

**권장:** A를 즉시(P0.5 패치, 별도 PR), B를 P1 안건 ①·③ 착수 전에. C는 행사용으로는 기각한다. 이유: 이 체험의 핵심 메시지는 "조건을 붙여 안건을 고치면 임원들이 움직인다"이고, 그 레버가 구조화된 조건이다. 자유 대화는 그 레버를 흐린다. B는 대화의 느낌을 주면서 레버를 유지한다.

## 4. B안 상세 — 회의록 타임라인

- **구성:** 상단 고정: 헤더·명패·진행 스트립·안건 한 줄. 본문: 타임라인(의장 브리핑 → 자료 4장(접힘 가능) → 핵심 쟁점 3개 → 임원 4명 첫 의견 → **[내 차례]** 추천 문구 quick reply + 작성기 → 내 발언 → 임원 반응(답글) → CIO의 후속 질문 + 빠른 답 3개 → 내 답 → 의장 "이 조건으로 안건을 고정합니다" → MOTION 화면으로 전환).
- **입력:** 내 차례 블록만 입력 가능. 나머지는 읽기 전용. quick reply는 현재 PhraseCard와 같은 복수 선택(체크)이며, 선택 시 작성기에 문장이 조합된다(현재 DraftEditor 재사용). 조건 칩은 작성기 아래 그대로.
- **live:** 임원 블록은 "판단 중…" 점 세 개 → 발언 도착 시 교체. 응답 실패는 "응답 없음"으로 남긴다(현재 상태 칩 재사용).
- **접근성:** 새 블록이 추가될 때 `aria-live="polite"`로 발화자와 첫 문장을 읽어 준다. 내 차례 블록으로 포커스 이동. 키보드만으로 완주하는 E2E 유지.
- **1280×720:** 자료 4장과 쟁점은 접힘 상태로 시작(제목만), 내 차례 블록이 항상 화면 안에 오도록 자동 스크롤.
- **관람 화면(P1):** 타임라인의 공개 블록만 복제하면 되므로 관람 뷰와도 잘 맞는다.
- **바뀌지 않는 것:** 단계 상태기계, reducer, 조건·표결 규칙, 240초·무입력 규칙, MOTION·VOTE·RESULT.

## 5. A안 카피 초안(시나리오 ② 기준, 사실 추가 없음)

- 의장 브리핑: "주간 보고마다 부서 자료를 손으로 모으고 있습니다. 이 일을 AI 업무 비서에 맡길지 정합니다. 도입할까요? 한다면 어떤 조건을 붙일까요? 이사님은 조건을 붙여 의견을 내시고, 마지막에 한 표를 던지시면 됩니다."
- 자료 한 줄: E1 "확정 수치 120건(검토 완료)", E2 "잠정 수치 126건(검토 전) — E1과 다르다", E3 "취합이 매주 반복된다 — 자동화 효과의 근거", E4 "권한·공유 범위가 아직 설계 중 — 도입 전 확인 필요".
- 핵심 쟁점: "① 숫자가 자료마다 다릅니다(E1·E2). ② 초안을 검토할 담당자가 없습니다(E3). ③ 권한과 공유 범위가 정해지지 않았습니다(E4)."
- 조건 미리보기 설명: "아래 네 가지를 조건으로 붙이면 임원들의 판단이 달라집니다."
- 후속 질문: "CIO가 묻습니다 — 자료마다 숫자가 다른데, 그대로 공유해도 될까요?"

## 6. 검토자에게 묻는 것

| # | 질문 | Codex(구현) | GPT(기획) |
| --- | --- | --- | --- |
| Q1 | A-1(의장 브리핑·자료 한 줄·쟁점 3개·조건 미리보기·진행 스트립)이 "새 사실을 만들지 않는다" 원칙을 지키는가 | 데이터 변경 범위 확인 | 문구 판정 |
| Q2 | A-2(후속 입력을 빠른 답 중심으로)가 P0 완료 기준(직접 입력만으로 완주 등)과 충돌하는가 | E2E 영향 | 시나리오 정합 |
| Q3 | B(타임라인)를 P1 전에 넣을 때 reducer·규칙을 건드리지 않고 가능한가, 접근성·240초 위험은 | 설계 검토 | 채택 여부·시점 |
| Q4 | C(완전 채팅형)를 기각하는 근거에 반론이 있는가 | live 지연·주입 관점 | 콘셉트 관점 |
| Q5 | 카피 초안(5절)이 C레벨 청중에게 적절한 어조인가 | — | 판정 |

## 7. 적용 순서(채택 시)

1. **T39 이해도 패치(A-1)**: 시나리오 데이터에 `chairBriefing`·`evidence[].insight`·`briefingIssues[]`·조건 미리보기 추가, BriefingScreen·진행 스트립·카피. 단위·E2E 갱신. 1~2일.
2. **T40 후속 단순화(A-2)**: ReactionsScreen 입력 재구성, 후속 질문 발화자 표시, 답글형 배치. 1일.
3. **T41 회의록 타임라인(B)**: BRIEFING~REACTIONS 통합 화면, live region, 1280×720 접기, E2E. P1 착수 전 3~4일.
4. 문서: SCENARIO_AI_ASSISTANT.md 브리핑·후속 절, CLAUDE_IMPLEMENTATION.md 화면표, DESIGN_SPEC.md 3장, FACILITATOR_GUIDE 검수 항목. 표결 규칙 절은 손대지 않는다.
