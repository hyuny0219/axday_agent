# BOARDROOM 2026 — Claude Code 구현 지시서

> **v0.8 개정 / 구현 지시서1.5:** [AGENT_BOARDROOM_SPEC.md](docs/AGENT_BOARDROOM_SPEC.md)가 임원 판단·AI 범위·시간 만료·개발 단계의 최우선 기준이다. 기존 고정 표결표와 동일 입력→동일 결과 검수는 scripted 전용이다. 현재 문서는 개발 요구이며 실제 연결 완료를 의미하지 않는다.

버전 1.6 · 2026-09-13 · 구현을 위한 제안 기본값 포함

> **v0.9 개정 / 구현 지시서1.6:** 브리핑 이해도(의장 브리핑·자료 해석·핵심 쟁점·조건 미리보기·진행 스트립)와 후속 입력 단순화를 P0.5로, 회의록 타임라인을 P1로 추가했다. 근거는 [v0.9 판정표](docs/REVISION_DECISIONS_v0.9.md). 표결 규칙·조건 ID·상태기계는 그대로다. (이 가운데 핵심 쟁점·조건 미리보기는 T52(2026-09-23)에서 제거했고 진행 스트립은 유지한다. 240초·무입력 타이머는 T50(2026-09-22)에서 제거했다 — 아래 3장 "세션 종료·리셋".)

## 1. 먼저 읽을 파일

0. [임원 에이전트·비서실장 명세](docs/AGENT_BOARDROOM_SPEC.md): v0.8 실행 모드·역할 판단·서버 응답·장애 처리 기준

1. 본 문서: 최신 기능 요구사항과 완료 기준
2. [상세 시나리오](docs/SCENARIO_AI_ASSISTANT.md): 첫 안건의 대사·조건·결과 규칙
3. [디자인 명세](docs/design/DESIGN_SPEC.md): 화면별 레이아웃·디자인 토큰·인터랙션
4. [개정 PPT](docs/reference/AX_Day_2026_Boardroom_Proposal.pptx): v0.7 · 18장, 8P AI 브리핑 / 9P 의견 입력 / 12P 최종 투표 / 13P 결과 / 15P 안건③ 최종안
5. 기존 `AX_Day_2026_Boardroom_Plan.md`: 세 안건의 전체 기획 배경

기존 기획서와 충돌하면 이 구현 지시서의 최신 UX를 따른다. 이미지 속 장식 문구보다 본 문서의 동작·접근성 규칙을 우선한다. 저장소에 별도 작업 지침이 있으면 먼저 읽고, 기존 앱·패키지·사용자 변경을 보존한다.

## 2. 구현 목표와 범위

삼성화재 AX Day C레벨 초청 행사에서 참가자가 **특별 이사로 이사회에 참여하는 4분 체험**을 만든다. 임원 네 명의 의견을 듣고 내 의견을 전달하며, 마지막에 한 표를 행사한다. AI는 자료·쟁점·발언 정리를 도와 업무 효율을 체감하게 한다.

확정된 요구사항:

- 현장 기본 환경은 PC·모니터·마우스다. 안건 선택, 추천 문구 복수 선택, 의견 전달, 다음 화면, 마지막 투표를 마우스 왼쪽 한 번 클릭으로 조작한다. 직접 입력·수정은 물리 키보드로 하며 추천 문구만으로 키보드 없이 완주한다. 더블클릭·드래그·롱프레스를 필수 동작으로 요구하지 않는다.

- CEO / CFO / CAIO / CISO / 나(특별 이사), 총 5석·각 1표.
- CFO와 CAIO는 별도의 두 사람이다(2026-09-19 개정). COO·CCO·CRO·CIO나 별도 전문 검토자·책임자 의석을 추가하지 않는다.
- 참가자는 안건 제안자·발표 평가 대상이 아니다. 임원과 함께 심의하는 구성원이다.
- 세 안건 중 하나를 선택한다. 한 체험에서 세 안건을 모두 진행하지 않는다.
- 토론 입력은 **추천 의견 문구 복수 선택 또는 직접 입력·수정**이다. 찬성·보류·반대를 고르는 투표 UI는 마지막 단계에만 나온다.
- 의견은 투표로 집계하지 않는다. AI가 참가자의 표를 추측하거나 대신 확정하지 않는다.
- 가결·보류·부결은 모두 정상적인 결말이다. 설득 성공률·정답 점수로 평가하지 않는다.
- 등장인물·안건·자료·표결 규칙은 행사 체험용 가상 설정이다. 실제 회사 이사회나 공식 발언을 재현하지 않는다.

구현 순서:

1. **P0:** 안건②의 실제 임원4명 판단·반응·최종표와 실제 AI ‘내 발언 정리’·현재 회의 의견 요약을 기본으로 구현한다. 별도 scripted 모드도 제공한다. 키 없이 데모는 실행되지만 live 완료를 대신하지 않는다. 관람 전송과③ 정규화는 P1이다.
2. **P1:** 안건①·③의 자료·문구·조건 정규화와 역할 프롬프트를 추가하고 관람 창을 구현한다. 기존 시나리오 표결표는 scripted 어댑터에서만 사용한다.
3. **P2:** 모델 비용·지연 최적화 및 선택적 출력·음성 확장. 실제 AI를 후속 선택 기능으로 미루지 않는다.

P0에서는 세 안건 카드를 보여주되 ①·③은 ‘준비 중’과 비활성 상태를 명시한다. 작동하지 않는 버튼으로 완성된 것처럼 보이지 않게 한다. P1 완료 후 세 카드 모두 활성화한다. P1에 별도 관람 뷰를 포함한다. P0의 관람 화면은 대기용 콘셉트·안건 소개 화면으로 운영한다. 참가자 화면 전체 복제를 기본으로 사용하지 않는다. 음성·아바타 영상·QR 서버·프린터 출력은 선택적 확장이다.

## 3. 화면과 상태 전환

| 상태 | 필수 표시 | 행동 / 다음 상태 |
| --- | --- | --- |
| ATTRACT | BOARDROOM 2026, 오늘 당신이 이사회의 한 자리를 맡습니다 | 체험 시작 → SELECT |
| SELECT | 세 안건 카드·4분·1개 선택 | 안건 선택 후 이사회 입장 → BRIEFING (시간 제한·타이머 없음, T50) |
| BRIEFING | 무대 명패(약칭 CEO·CFO·CAIO·CISO·나)·의장 브리핑(상황·결정 질문·내 역할)·오른쪽 열은 결정 질문(최대) → 현재 상황·제안·아직 정하지 않은 것 → 특별 이사님이 할 일 + "최종 결정: 승인·보류·부결" → 가상 자료 4장(자료명·해석·원문 상시 노출, 자료 ID는 화면에 쓰지 않음). 핵심 쟁점 카드·조건 미리보기·"체험용 사전 구성" 배지는 T52(2026-09-23)에서 제거 | 의견 듣기 → OPINIONS |
| OPINIONS | 임원 4명의 한 줄 의견, 펼치면 근거 | 내 의견 말하기 → DISCUSS |
| DISCUSS | 추천 문구 체크박스·직접 입력·AI 비서실장 | 의견 전달 → REACTIONS |
| REACTIONS | 내 발언 인용 아래 임원 반응(답글형, 변한 임원 강조)·CAIO의 후속 질문 1회 + 빠른 답 3개(직접 입력은 접힘, 열면 300자 입력) | 답 선택 또는 보완 의견 전달 또는 앞선 의견 유지 → MOTION |
| MOTION | 최종 안건·수정 조건·남은 확인 사항 | 이 안건으로 표결 → VOTE; motion 고정 |
| VOTE | 찬성 / 보류 / 반대 단일 선택·최종 투표 확정 | 참가자 확정 후 임원표 수신 대기(최대 8초) → RESULT |
| RESULT | 결론·동등한 5개 의석·내 의견 반영·남은 과제·자동 자료 정리 데모 표시 기록 및 추가 AI 도움 | 체험 종료 → ATTRACT; 세션 초기화 |

권장 경과 시간: 0:00–0:50 착석·브리핑, 0:50–1:30 임원 의견, 1:30–2:50 내 의견·후속 토론, 2:50–3:20 최종 안건·투표, 3:20–4:00 결과. 단계별 강제 대기 없이 빠르게 이동할 수 있다.

### 세션 종료·리셋 (T50, 2026-09-22 개정)

- 세션은 시간으로 끝나지 않는다. 240초 절대 deadline, 남은 시간 안내, 시간 만료 시 원안 자동 고정, 무입력 75초 안내·90초 ATTRACT 복귀는 모두 T50에서 제거했다(최우선 명세 `AGENT_BOARDROOM_SPEC.md` 6장). 세션을 끝내는 경로는 결과 화면의 "체험 종료"와 운영 메뉴의 "새 체험"·"scripted로 새 체험"뿐이다.
- 최종 안건은 참가자가 "이 안건으로 표결"을 눌러야만 고정된다. 자동 고정 경로는 없다.
- 참가자 최종 표가 확정되지 않았으면 UNCAST(미표결). 라디오 선택만 한 값·토론 발언은 표로 사용하지 않는다.
- live 임원은 동일한 고정 안건에 각자 판단해 투표한다. 최종표는 고정 직후 병렬 요청하고 참가자 확정과 함께 기다리되 8초를 넘지 않는다. 그 안에 검증·확정된 표만 집계하고 나머지는 UNCAST다. scripted만 규칙표를 사용한다. 종료·초기화된 세션에 늦게 도착한 AI 응답은 폐기한다.
- 결과 화면에 열람 제한은 없다. 참가자가 "체험 종료"를 누르거나 운영자가 초기화할 때까지 유지되며, 방치된 결과 화면은 진행 요원이 초기화한다(FACILITATOR_GUIDE 지표 "결과 화면 초기화").
- 운영자 새 체험은 진행 중이면 한 번 확인하고 모든 세션 상태·입력·조건·표·비동기 요청을 초기화하며 오래된 응답을 무시한다. 새 참가자에게 이전 입력을 노출하지 않는다.
- 새로고침 기본 동작은 새 세션. 영구 저장 없이 메모리 상태로 시작한다.

### 현장 운영 — P0 구현, 행사 장비에서 최종 검증

- 무입력 자동 복귀(75초 안내·90초 ATTRACT 복귀·SELECT 포함)는 T50에서 제거했다. 자리를 비운 참가자의 화면은 진행 요원이 운영 메뉴 "새 체험"으로 초기화한다(FACILITATOR_GUIDE). 초기화는 중도 종료이며 미확정 표를 집계하거나 결과 화면을 만들지 않는다.
- 운영자 메뉴: 우측 상단에 표시된 ‘운영’ 버튼을 마우스로 한 번 클릭해 연다. 메뉴에 ‘새 체험’, ‘전체화면 진입’, ‘전체화면 종료’, ‘닫기’를 제공한다. P1에서는 ‘관람 창 열기’를 추가한다. ‘새 체험’은 현재 상태 삭제 확인 후 실행한다. 이는 현장 운영 편의 메뉴이며 보안 인증 기능이 아니다.
- 전체화면은 운영자의 명시적 버튼 클릭에서 Fullscreen API로 요청한다. 거부·미지원이면 브라우저 전체화면/관리형 키오스크 모드 안내와 닫기를 제공한다. Escape로 이탈해도 상태는 보존한다. 현장 운영자는 OS·브라우저 키오스크 모드와 자동 실행을 장비별로 설정·검증한다.
- 한글 폰트·아이콘·배경·이미지를 라이선스와 함께 앱에 로컬 번들한다. CDN을 필수로 두지 않는다. 모델 연결을 끊어도 데모는 동작하며, 인터넷 없이 로컬 서버에서 새로고침·재시작 후 모든 자산이 로드되어야 한다.
- 실제 행사 PC의 마우스와 물리 키보드에서 한글 조합·수정·삭제·300자 경계·줄바꿈을 검증한다. 조합 중 Enter는 제출하지 않는다. 의견은 ‘의견 전달’ 버튼 클릭으로 제출한다. OS 터치 키보드와 롱프레스 검수는 기본 행사 완료 조건에서 제외한다. 터치 단말을 도입하면 해당 환경 검수를 추가한다.

### P1 관람 뷰

`?view=spectator`는 별도 대형 화면용 읽기 전용 뷰다. 기본 지원 환경은 PC 한 대·모니터 두 대 확장 모드·동일 브라우저 프로필·동일 origin이다. 같은 저장소 파티션과 채널명을 공유하는 BroadcastChannel로 공개 데이터만 동기화한다. localStorage에는 참가자 원문이나 세션 데이터를 저장하지 않는다. 관람 뷰는 진행·조건·표결을 수정할 수 없다.

P1 운영 메뉴의 ‘관람 창 열기’를 명시적으로 클릭하면 관람 URL을 `window.open`으로 연다. 동일한 이름의 관람 창을 재사용해 중복 개설을 피한다. 두 번째 모니터로 옮긴 뒤 관람 창에 표시된 전체화면 버튼을 클릭한다. 관람 창의 전체화면 버튼은 화면 표시만 조작하며 세션 변경 권한은 없다. 팝업 차단 시 관람 URL과 동일 프로필의 새 탭에서 열어 분리하는 대안을 안내한다. 참가자 창을 관람 URL로 덮어쓰지 않는다.

서로 다른 user-data-dir/프로필, 일반 창과 시크릿 창의 혼합, 다른 브라우저 조합은 기본 지원에서 제외한다. 같은 PC에서 프로필 분리가 반드시 필요하면 localhost WebSocket 릴레이를 선택 확장으로 검토한다. 전송 어댑터 인터페이스를 분리해 공개 payload·revision·heartbeat 규칙은 유지한다. 실제 릴레이 구현은 필요가 확인된 경우에만 수행한다.

별도 PC는 별도 네트워크 구성이다. localhost 릴레이만으로 다른 PC가 연결되지는 않는다. 네트워크로 접근 가능한 서버 주소와 연결·접근 설정을 마련하고 별도 검수해야 하며 기본 P1 완료 조건에는 포함하지 않는다.

기본 현장 검수는 두 모니터의 각각 전체화면 상태에서 동기화·초기화·재접속·브라우저 재시작 후 운영 메뉴를 통한 복구다. 장비에서 동시 전체화면이 어려우면 관람 창 전체화면+참가자 창 최대화를 운영 대안으로 검증하고 기록한다.

공개 payload는 sessionId, revision, stage, 공개 안건명, 사전 승인된 임원 의견/반응 ID, 참가자가 확인한 조건의 사전 작성 라벨, 최종 확정 집계·결론만 허용한다. 발언 원문·draft·AI 초안·원문을 인용한 임원 문장은 전송하지 않는다. 화면에서 감추는 것만으로 처리하지 않는다.

DISCUSS·REACTIONS에는 공개용 임원 의견과 ‘특별 이사 논의 중’을 표시한다. VOTE에는 ‘표결 중’을 표시하고 선택 중인 표는 보내지 않는다. RESULT에서만 확정 표·결론을 공개한다. 공개 조건 라벨은 최종 안건 확인 이후에만 표시한다.

참가자 앱은 2초 간격 heartbeat, 관람 뷰는 시작/재연결 시 최신 공개 snapshot을 요청한다. 5초 동안 heartbeat가 없으면 이전 내용을 지우고 대기 화면으로 전환한다. reset은 sessionId를 바꾸고 reset 이벤트를 보낸다. 이전 세션·낮은 revision 메시지는 무시한다. 새로고침·창 재실행·진행 중 연결·세션 교체·연결 끊김에서 이전 내용 잔류와 잘못된 결과 공개를 검사한다. 이 시간은 구현 기본값이며 현장 조정 가능하다.

1920×1080 대형 화면과 실제 설치 위치 약3m에서 안건·임원 역할·집계·결론 판독을 리허설한다. P0 전체 화면 복제는 예외 시연으로만 사용하며 공개 범위를 참가자에게 먼저 알리고 원문 입력 중 노출을 중단한다.

## 4. 의견 입력의 정확한 동작

서기 입력을 허용한다. 진행 요원이 참가자의 말을 그대로 입력해 읽어 확인받고, 조건 확인·의견 전달·최종 투표는 참가자가 직접 클릭한다. 300자를 넘으면 참가자가 축약 표현을 결정하며 서기가 임의로 요약하지 않는다. [진행 가이드](docs/FACILITATOR_GUIDE.md)를 따른다.

추천 문구는 3~6개의 체크박스 카드로 표시한다. 선택하면 본문의 ‘내 발언’ textarea에 문구를 조합한다. 사용자는 선택 없이 직접 입력할 수도 있다. 최대 300자, 현재 글자 수 표시, 공백만 있으면 전달 비활성화.

편집 손실 방지:

- `selectedPhraseIds`와 `draftText`를 분리한다. 실제 제출되는 값은 항상 화면에 보이는 `draftText`다.
- 직접 수정 전에는 체크 변경으로 draft를 자동 구성한다.
- 직접 수정 이후 체크를 바꾸면 ‘직접 쓴 내용 유지 / 선택 문구로 다시 구성’ 작은 확인 UI로 처리한다. 기본은 유지이며 기존 입력을 조용히 덮어쓰지 않는다.
- 입력 수정으로 기존 체크 문구의 의미를 바꿨다면 과거 체크 ID를 판정 근거로 재사용하지 않는다. 제출 시 현재 텍스트를 기준으로 다시 해석한다.
- IME 조합 중 Enter가 제출되지 않게 한다. 명시적인 ‘의견 전달’ 버튼으로 전송한다.
- 후속 토론은 최대 1회 보완. ‘앞선 의견 유지’로 추가 입력 없이 진행 가능하다.

**자유 입력 처리:** 원문은 그대로 발언 기록에 남긴다. 조건 자동 추출은 확정이 아닌 제안이다. 서버/데모 해석기가 찾은 허용 조건을 칩으로 보여주고 ‘의견 전달’에 앞서 같은 화면에서 선택·해제할 수 있게 한다. 해석할 수 없는 문장은 ‘말씀은 회의 기록에 남깁니다. 반영할 조건이 있으면 선택해 주세요’로 안내한다. 조건을 고르지 않아도 원문 전달 및 진행 가능하다.

직접 입력 모드의 조건 확인은 서버에 맡긴 임의 결정이 아니라 참가자가 확인한 구조화 입력이다. 추천 문구를 수정하지 않고 보낼 때는 문구에 정의된 조건을 미리 보여주고 같은 전달 버튼으로 확인한다. 부정문·상충 문구를 단순 키워드로 자동 확정하지 않는다.

반영 여부는 `proposed → confirmed → includedInFinalMotion` 단계로 기록한다. 후속 보완은 누적 조건을 보여주고 유지·해제하게 하며, 충돌 조건은 동시 확정할 수 없도록 한다.

## 5. AI 비서실장

BRIEFING에서 세 안건 모두 자료 4장 옆의 AI 정리 카드 1장을 버튼 조작 없이 상시 표시한다. 카드에는 ‘체험용 사전 구성’과 E1~E4 근거 연결을 표시한다. 강제 영상 대기 없이 바로 읽고 진행한다. 각 시나리오의 자동 정리 문구를 사용한다.

추가 도움은 모든 토론 화면에서 선택적으로 연다. PC 사이드 패널이며 ‘닫기’가 항상 보인다.

| 기능 | 출력 | 참가자 행동 |
| --- | --- | --- |
| 자료 자동 정리 | 자료 4장의 핵심·차이·확인 항목, 근거 ID, 체험용 사전 구성 표기 | BRIEFING에서 상시 표시; 추가 클릭 불필요 |
| 의견 한눈에 보기 | 4명 의견의 공통점·쟁점·근거 ID | 원래 의견 카드로 이동 |
| 조건 비교하기 | 현재 원안과 제안 조건의 차이, 남은 확인 사항 | 조건 선택에 참고 |
| 내 발언 정리 | 원뜻·유보·부정 표현을 유지한 300자 이내 초안 | 내 발언에 적용 버튼으로만 textarea 반영 |

데모 모드는 사전 작성 콘텐츠로 즉시 반응하고 ‘체험용 사전 구성 데모’ 표시. 임의의 절감률·실제 AI 분석 표현을 만들지 않는다. 실제 모델 모드도 원문·근거 ID·허용 조건만 전달하고 출력 schema를 검증한다. unknown evidence/condition ID, 빈 응답, timeout(5초), 네트워크 오류는 도움 기능의 기본 안내로 전환한다. 특히 내 발언 정리는 아래 실패 처리에 따라 참가자 원문을 보존하며 사전 문장으로 대체하지 않는다.

자동 카드가 실제 화면에 렌더되면 세션당 한 번 `BRIEFING_SUMMARY_SHOWN`(mode=`scripted`, evidenceIds, shownAt)을 기록한다. 정상 완주 결과의 ‘AI가 도운 일’에는 항상 ‘자료 4장 자동 정리 데모 표시’를 포함한다. 추가 기능을 쓰지 않았으면 ‘추가 AI 도움은 사용하지 않았습니다’를 함께 표시한다. 실제 모델 호출·추가 데모 요청은 별도 이벤트로 구분한다. 표시를 실제 이해·효과 측정·모델 호출로 과장하지 않는다. 비정상 경로로 표시되지 않았다면 표시 기록을 만들지 않는다.

비서실장 출력은 임원 표·최종 안건·참가자 표를 직접 수정할 수 없다. 임원 에이전트만 본인의 최종표를 제안하고 서버가 검증·확정한다. 요청에는 sessionId/requestId를 붙이고 리셋·종료 뒤 응답을 무시한다. 실제 API 인증정보는 서버 환경변수에만 둔다. P0 scripted는 키 없이 완주하고 live는 서버 모델 연결로 별도 검증해야 한다.

### 실제 AI 범위와 실패 처리

행사 네트워크·서버 측 인증·허용 모델이 확정된 경우 임원4명의 판단·토론·표결과 ‘내 발언 정리’ 및 회의 의견 요약을 live로 제공한다. 참가자 원문과 필요한 근거 본문·허용 ID를 입력하고 300자 이내 초안·근거 ID를 받는다. ID만 보내서 근거 내용을 추정하게 하지 않는다. 스키마 검증은 형식 검사이며 사실성·의도 보존을 보장하지 않는다. 부정·유보·숫자·핵심 조건을 바꾸지 않는지 확인하고 원문/초안을 함께 보여준다. ‘내 발언에 적용’ 클릭 전에는 원문을 교체하지 않는다.

5초 timeout·빈 응답·형식 오류 시 원문을 유지하고 ‘정리하지 못했습니다. 원문으로 진행할 수 있습니다’를 표시한다. 일반 사전 문장을 참가자의 발언처럼 덮어쓰지 않는다. 늦은 응답은 폐기한다. 성공·실패·적용 여부를 구분하고 mode=live/scripted를 결과 기록에 남긴다. 네트워크가 없으면 모두 scripted이며 ATTRACT/RESULT에 ‘사전 구성 시뮬레이션’, 도움 기능에 ‘AI 비서실장(시연)’을 읽기 쉬운 크기로 표시한다.

## 6. 데이터·엔진 설계

기존 스택이 있으면 유지한다. 새 프로젝트라면 React + TypeScript + Vite 웹 앱을 기본 구현안으로 사용한다. 라이브러리 버전은 작업 시 호환되는 안정 버전을 확인하고 lockfile을 커밋한다. 실제 AI API는 별도 서버 어댑터로 분리한다.

권장 책임 분리: `content/scenarios`(자료·대사), `domain`(상태·조건·표결), `services/assistant`(보좌 기능), `services/boardAgents`(역할별 모델 호출), `services/orchestrator`(순서·시간·검증), `components`(화면), `styles`(디자인 토큰). UI 컴포넌트에 표결 규칙을 넣지 않는다.

```ts
type Vote = 'YES' | 'HOLD' | 'NO' | 'UNCAST';
type MemberId = 'ceo' | 'cfo' | 'cio' | 'ciso' | 'participant';
interface Opinion {
  id: string;
  originalText: string;
  selectedPhraseIds: string[];
  confirmedConditionIds: string[];
  createdAt: number;
}
interface Motion {
  id: string;
  scenarioId: string;
  kind: 'original' | 'amended';
  conditionIds: string[]; // 참가자가 확인한 변경 ID
  baseConditionIds: string[];
  effectiveConditionIds: string[]; // 원안 + 추가 - 명시적 삭제
  executionMode: string; // 안건③의 확대/선검증/반복검증 등
  frozenAt: number;
}
interface Ballot { memberId: MemberId; motionId: string; vote: Vote; confirmedAt: number | null }
```

P0는 위 Motion 인터페이스를 유지하되 `baseConditionIds = []`, `effectiveConditionIds = [...conditionIds]`, `executionMode = 'DEFAULT'`로 구성한다. conditionIds 자체도 확인된 입력의 복사본으로 저장한다. 두 배열은 같은 참조를 공유하지 않고 고정 후 수정하지 않는다. 평가기 입력은 처음부터 effectiveConditionIds와 executionMode다. P1의 ③ 원안 병합·명시적 삭제·실행 방식 정규화는 시나리오 정규화 함수에 추가하며 Motion 인터페이스를 바꾸지 않는다.

세션에는 stage / sessionId / mode / scenarioId / startedAt / draft / opinions / followUpUsed / assistantActions / transcript / roleStatus / execBallotsPending / finalMotion / ballots / outcome / pendingVote를 보관한다(`src/domain/types.ts` `Session`). `deadline`·`lastActivityAt`은 T50에서 타이머와 함께 제거했다. 코드 식별자는 영어, 화면 문구는 한국어를 사용한다.

원안의 기본 조건은 추천 문구 체크 여부와 무관하게 유지한다. kind는 조건 배열 길이가 아니라 원안과 의미 있는 차이가 있는지로 결정한다. 동일한 원안을 재확인한 발언은 original이다. 시나리오③은 변경 ID를 유효 조건·실행 방식으로 정규화한 뒤 규칙을 적용한다. scripted 우선순위 표는 처음 참인 행을 적용한다. live는 정규화한 최종안 본문·조건·논의를 임원에게 제공해 판단을 받는다. Ballot 응답 메타데이터는 에이전트 명세5장을 적용한다.

최종 안건 ID가 없는 투표, 잘못된 안건 ID에 대한 투표, 중복 의석, 확정 후 재투표는 엔진에서 차단한다. UI 비활성화만으로 처리하지 않는다. YES≥3 가결, NO≥3 부결, 그 밖은 보류. UNCAST는 HOLD와 별도 집계하고 전체 의석 5를 유지한다. double-click·리렌더로 중복 확정하지 않는다.

## 7. 구현 완료 기준

### P0 공통 흐름·안건②

- P0 전체 흐름을 실제 브라우저에서 조작 가능. 추천 문구만으로 키보드 없이 완주하고 직접 입력만으로도 완주 가능.
- 추천 문구 복수 선택, 300자 제한, 편집 보존, 공백 방지, IME 조합 입력 확인.
- 최종 투표 이전에는 찬성/보류/반대 선택 버튼 없음. 토론에서 전달해도 ballot은 생성되지 않음.
- 5명 각 1표, 고정된 동일 안건 ID. 참가자 표는 확정 버튼으로만 성립하고 라디오 선택만으로는 UNCAST. 임원 표는 8초 안에 도착·검증된 것만 집계하고 나머지는 UNCAST.
- scripted의 가결·보류·부결·내 표 영향은 고정 테스트로 검증한다. live는 근거·역할·논거 반영·안건 동일성·응답 실패를 검수하며 고정 득표수를 요구하지 않는다.
- ~~무입력 75초 안내·90초 복귀, 계속 버튼, RESULT 진입 후 타이머 재시작, 240초와 동시 만료 우선순위~~(T50, 2026-09-22 제거 — 체험은 시간으로 끝나지 않는다. `AGENT_BOARDROOM_SPEC.md` 6장). 운영 버튼 클릭·초기화 확인 메뉴, scripted로 새 체험, 모델 연결 확인, 전체화면 거부 처리를 확인.
- AI timeout, 늦은 응답, 새 체험, 두 번 클릭에서 상태가 일관됨.
- 추가 AI 버튼을 전혀 누르지 않은 완주에서도 BRIEFING 요약 카드와 결과의 자동 정리 표시 기록이 있음. 자동 데모/추가 데모/실제 호출을 구분. 내 의견이 채택되지 않았는데 채택됐다고 쓰지 않음.
- 현장 마우스·물리 키보드 한글 입력 및 오프라인 로컬 자산 검증을 완료하고 장비·OS·브라우저·미검증 항목을 기록.
- 1280×720에서 추천 문구 6개·300자 입력·최종 조건 4개를 실제 콘텐츠로 배치해 캡처. 아바타는 전 화면 비실사 아이콘/이니셜로 통일.
- 1920×1080 및 1280×720에서 주요 CTA와 입력이 잘리지 않음. 모바일·터치 단말 대응은 선택 확장으로 두며 P0/P1 완료 조건에 포함하지 않는다. PC의 키보드 접근성과 200% 확대 대응은 유지한다.
- PPT와 대비하여 네이비·시안·임원 카드·내 발언·최종 투표 화면의 시각적 일관성 확인. 최소 안건 선택/토론/투표/결과 스크린샷 저장.
- README에 설치·개발·빌드·테스트 방법, 데모/실제 AI 차이, 완료 범위와 미구현 항목 작성.

테스트는 표결·상태 전환의 단위 테스트와 주요 E2E 경로를 우선한다. UI 문구를 그대로 복제하는 불필요한 테스트는 만들지 않는다. P0 완주 후 P1로 확장하고, 실제 AI 키·현장 장비가 없어 검증하지 못한 live P0 항목을 명확히 보고한다. 실행되지 않는 골격 코드만으로 완료라고 보고하지 않는다.

### P0.5 이해도·후속 단순화 (v0.9, 안건②)

- BRIEFING에 의장 브리핑 3문장·결정 질문·현재 상황·제안·아직 정하지 않은 것·특별 이사님이 할 일·자료 4장(자료명·해석 한 줄·원문 상시 노출)이 있고, 모두 시나리오 데이터에서 온다(화면 코드에 문구 하드코딩 없음). 핵심 쟁점 3개·조건 미리보기는 T52(2026-09-23)에서 제거했다 — 자료 카드 해석과 내용이 겹쳐 같은 말을 두 번 읽게 했기 때문이며, 복원 대상이 아니다. 진행 스트립(`ProgressStrip`, ① 상황 파악 → ⑤ 표결)은 유지하며 BRIEFING에서 ①, DISCUSS에서 ③을 가리킨다(`e2e/briefing.spec.ts`). 새 사실(확정 수치·비율·절감률)을 만들지 않는다.
- REACTIONS 후속 질문에 발화자(안건 ② 사내 게시판 익명제에서는 CFO — 신고 처리 담당자 질문, `docs/SCENARIO_ANON_BOARD.md`)가 표시되고, 빠른 답 3개만으로 MOTION까지 완주할 수 있다. 직접 입력만으로 완주하는 경로도 유지된다(접힌 편집기를 마우스·키보드로 열어 입력·제출). 두 경로 모두 E2E로 확인한다.
- 처음 보는 사람 3명 전원이 설명 없이 BRIEFING을 읽고 (1) "무엇을 정하는지"(사내 게시판을 익명제로 바꿀지와 붙일 조건)와 (2) "내가 할 수 있는 일"을 한 문장으로 말하고, 스스로 REACTIONS까지 진행한 뒤 (3) "두 번째 입력이 CFO의 신고 처리 담당자 질문에 답하는 것"임을 말한다. 이 검수는 T52·T53(2026-09-23) 뒤 새 화면으로 다시 받는다. 한 명이라도 실패하면 해당 카피(T39 또는 T40)를 수정해 재검수한다(현장 검수, FACILITATOR_GUIDE "v0.9 이해도 검수").

### P1 추가 범위

- 회의록 패널(v1.0 개정, 원래 v0.9 B안 타임라인): 무스크롤 조종석과 충돌하는 스크롤 통합 대신 왼쪽 열 무대 아래 창 고정 패널로 "누가 무엇을 말했는가"를 한 줄씩 쌓는다(DESIGN_SPEC v1.0 7절). 아래 문장 중 스크롤·포커스 이동 조건과 무입력 활동 조건(T50)은 소멸했고 나머지는 유효하다. 원문: BRIEFING~REACTIONS를 한 스크롤 타임라인으로 통합한다. 의장·임원·나의 발언이 시간순으로 쌓이고 내 차례에 추천 문구가 빠른 답으로 뜬다. live에서는 임원 후속 라운드의 promise가 settle된 뒤에만 안건 고정 CTA를 활성화한다(별도 벽시계 타이머 금지). 라운드별 실패 상태는 `SET_ROLE_STATUS`의 선택 필드 `stage`로 화면 쪽 roundLog에 남기고 reducer는 읽지 않는다. 프로그램 스크롤은 무입력 활동으로 세지 않는다. 새 블록은 `aria-live="polite"`로 읽어 주고 내 차례 블록으로 포커스를 옮긴다. 단계 상태기계·조건·표결 규칙·MOTION 이후 화면은 그대로다. 상세는 [v0.9 UX 수정안 4절](docs/REVISION_PROPOSAL_v0.9_UX.md).

- ①은 찬성·반대 한 표로 가결·부결을 성립시키는 경로, ③은 찬성으로 가결을 성립시키거나 다른 표로 보류가 되는 경로를 확인한다. ③에서 참가자 반대로 부결3표를 만드는 경로는 요구하지 않는다.
- ③ 실행 방식 정규화와 MOTION/RESULT의 안건 종류 표시를 확인한다. 추천 카드 꼬리표는 제안 유형이며 승인 결과가 아니다.
- P1 관람 뷰에 원문·AI 초안·미확정 표가 전송되지 않음. 초기화·재접속·5초 연결 끊김 처리와 실제 거리 가독성 확인. 동일 프로필의 관람 창 열기·팝업 차단 대안·두 모니터 배치·전체화면 또는 최대화 대안·재시작 후 복구를 검사한다.
- 서기 입력 후 참가자 확인 흐름을 리허설. 참가자가 직접 확정한 표결 완료와 운영자 초기화(중도 종료)를 구분해 측정.
### P2 및 선택 범위

- 원뜻 보존·명시적 적용·5초 실패 후 원문 유지·live/scripted 기록은 P0 필수 검수다. P2는 비용·지연 최적화와 선택 확장이다.
- 결과 출력은 장비 확정 전 필수 범위가 아니다. 선택 시 인쇄 실패 후에도 결과 열람·종료가 가능해야 하며 재출력은 운영자가 제어한다. 사전 제작 특별 이사 명패를 기본 기념물로 제안한다.

## 8. Claude Code에 전달할 시작 프롬프트

> 이 저장소의 CLAUDE_IMPLEMENTATION.md, docs/SCENARIO_AI_ASSISTANT.md, docs/design/DESIGN_SPEC.md와 참조 이미지를 읽고 BOARDROOM 2026 웹 체험을 구현해줘. 먼저 안건 ② P0를 실제 조작 가능한 상태로 완성하고 테스트한 뒤 P1의 두 안건으로 확장해줘. 현장 조작은 PC 마우스 클릭 중심이고 직접 입력은 물리 키보드로 해. 참가자는 특별 이사이고 토론은 추천 문구 복수 선택 또는 직접 입력이며, 투표는 마지막에만 해. PPT의 미래적인 네이비·시안 회의실 디자인을 실제 반응형 UI로 구현해줘. P0부터 실제 임원4명 에이전트와 실제 내 발언 정리를 AGENT_BOARDROOM_SPEC.md에 따라 연결하고, 별도 scripted 데모는 키 없이 실행하되 live 완료와 구분해.  기존 저장소 파일과 사용자 변경은 보존해줘. 완료 후 실행 방법, 검증 결과, 화면 캡처, 남은 제약을 정리해줘.
